$(document).ready(function(){
	$('.penthouse').slick({
		arrows: false,
		variableWidth: true,
	});
	$('.features__list').slick({
		arrows: false,
		variableWidth: true,
	});
	$('.house__list').slick({
		arrows: false,
		variableWidth: true,
	});
});